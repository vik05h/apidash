name: mcp
description: Model Context Protocol (MCP) server implementation for APIDash, enabling LLMs to access and execute API requests.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.8.0

dependencies:
  json_annotation: ^4.9.0
  meta: ^1.16.0
  uuid: ^4.5.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  json_serializable: ^6.9.4
  test: ^1.25.2
